# AGENTS.md — 项目说明与协作约定

本文件写给在这个文件夹里工作的 AI 助手（也写给未来的自己）。

---

## 一、和用户怎么沟通（最重要）

- 用户对**现在流行的编程工具**和 **AI 基础知识**都不大懂，遇到这类内容必须用**大白话**解释。
- 少用行话；非用不可时，先说明它是什么意思、会带来什么后果，再往下讲。
- 不只讲"怎么做"，还要讲清"为什么"和"有什么风险"。

---

## 二、这个项目是什么

- **名称**：`gitshiyan`，意思就是"git 实验"。
- **性质**：一个专门用来**练习 Git 操作**的仓库。不是实际开发项目——没有业务代码、没有依赖清单、没有测试、没有构建脚本。
- **远端仓库**：`git@github.com:cicitivi-zwj/gitshiyan.git`（SSH 方式）。
- **默认分支**：`main`。

---

## 三、目录与文件

| 路径 | 说明 |
| --- | --- |
| `README.md` | 项目说明，目前只是骨架，还没细化 |
| `AGENTS.md` | 本文件，写给 AI 助手的说明 |
| `.gitignore` | 忽略规则：系统垃圾、IDE、缓存、密钥、办公文档等 |
| `.gitattributes` | 换行符策略：文本统一 LF；`.bat` / `.cmd` / `.ps1` 保留 CRLF |
| `.editorconfig` | 编辑器格式约定（编码、缩进、行尾） |
| `tijiaoshiyan.txt` | 练习"提交"用的试验文本（3 行中文） |
| `wendang/` | 存放文档的目录 |
| `wendang/科技共和国_中文版.md` | 《科技共和国》中文版全文，约 517 KB |
| `wendang/科技共和国_英文版.md` | 《科技共和国》英文版全文，约 625 KB |
| `app/` | **空目录**（Git 不记录空目录，所以远端的网页上看不到它） |
| `node_modules/` | 依赖目录，已被忽略，不入库 |
| `.worktrees/` | 本地工作树目录，已被忽略，不入库 |

仓库里**真正被 Git 跟踪的文件只有 8 个**：`.editorconfig`、`.gitattributes`、`.gitignore`、`AGENTS.md`、`README.md`、`tijiaoshiyan.txt`，以及 `wendang/` 下的两个 md 文件。

---

## 四、分支与工作树

| 分支 | 说明 |
| --- | --- |
| `main` | 主线 |
| `fenzhi2` | 练习分支 |
| `fenzhi2.1` | 练习分支，已检出到独立工作树 |
| `fenzhi2.2` | 练习分支，已检出到独立工作树 |

**工作树（worktree）是什么**：同一个仓库**同时在好几个文件夹里打开**，每个文件夹停在不同分支上，省去来回切换分支的麻烦。

现在的工作树：

| 文件夹 | 检出分支 |
| --- | --- |
| `F:\GIT` | `main` |
| `F:\GIT\.worktrees\fenzhi2.1` | `fenzhi2.1` |
| `F:\GIT\.worktrees\fenzhi2.2` | `fenzhi2.2` |

注意事项：

- **同一条分支不能同时检出到两个工作树**。
- 不要直接删工作树文件夹，要用 `git worktree remove <路径>`，否则会在 `.git` 里留下垃圾记录。

---

## 五、协作约定

- **提交信息**加前缀，方便一眼看懂每次改了什么：`feat:`（新功能）、`fix:`（修问题）、`docs:`（文档）、`chore:`（杂务 / 配置）。
- **换行符**由 `.gitattributes` 统一管理：文本文件在仓库里统一存成 LF。在 Windows 上提交时可能看到 "CRLF will be replaced by LF" 的提示，这是正常现象，不用管。
- **敏感内容一律不要提交**：激活码、序列号、注册码、密码、密钥、凭证，以及 `*.docx` / `*.xlsx` 等办公文档——`.gitignore` 已经把这些拦在外面。
  - 确实需要强行加入某个被忽略的文件：`git add -f <文件>`。
  - 需要给某个被忽略的文件放行：在 `.gitignore` 里加一行例外，例如 `!docs/设计文档.docx`。
  - **关键**：`.gitignore` 只对"还没被跟踪"的文件生效；已经进了仓库的文件要移出来，得用 `git rm --cached <文件>`。

---

## 六、历史教训（务必遵守）

这个仓库上发生过两次"误把东西推到公开位置"的事故：

1. 误提交并推送了一份 AdGuard 激活码（`.docx` 文件）。
2. `AGENTS.md` 里写过用户的真实姓名。

两次都是通过**重写历史 + 强制推送 + 清理本地对象**才彻底清除的。由此得出几条规矩：

- **删文件 ≠ 删历史**。文件删掉后，旧版本仍留在提交记录里，翻历史、搜索都还能找到。
- 要真正清除，必须：重写历史（如果只是最近几次提交，可以把相关提交合并或丢弃）→ 强制推送 → `git reflog expire --expire=now --all` → `git gc --prune=now`。
- **强制推送会改写远端历史**，只有确认"这个仓库只有自己一个人在用"时才可以做；做完还要如实告诉用户：GitHub 服务器上被替换掉的旧提交可能仍以"孤立对象"形式残留一段时间，若要求百分之百清除，需联系 GitHub 官方。
- 最省事的办法永远是：**一开始就别提交敏感内容**。
